language: scala

scala:
  - 2.11.8

jdk:
  - oraclejdk8

dist: trusty

sbt_args: -J-Xss2M -J-Xms4G -J-Xmx4G

script:
  - sbt ++$TRAVIS_SCALA_VERSION clean test scalastyle test:scalastyle
  - sbt ++$TRAVIS_SCALA_VERSION corenlp:test
